# AIpathCut 操作使用指南

## 一、软件简介

AIpathCut 是一款专为切割机设计的图形路径生成工具，支持从白底图片中提取人物/动物轮廓（针对动漫人物优化），并生成切割机专用的G代码。

## 二、系统要求

- **操作系统**: Windows 10/11
- **Python**: 3.8 或更高版本
- **依赖库**: OpenCV, NumPy, Shapely, Pillow, tkinterdnd2

## 三、安装与启动

1. **安装依赖**（首次使用）：
   ```bash
   pip install opencv-python numpy shapely pillow tkinterdnd2
   ```

2. **启动软件**：
   ```bash
   python gui.py
   ```
   或直接双击 `gui.py` 文件（如果已配置Python关联）

## 四、界面布局

```
┌─────────────────────────────────────────────────────────────┐
│                     描边提取工具                              │
├──────────────────────┬──────────────────────────────────────┤
│       原图           │           描边结果                    │
│   (点击或拖入图片)    │       (等待处理...)                   │
│                      │                                       │
├──────────────────────┴──────────────────────────────────────┤
│                        参数设置                               │
│  白色阈值: [====] 230  |  偏移距离: [====] 0.0 (正值外扩)    │
│  加工宽: 50 mm         |  加工高: 76 mm   |  进给: 100 mm/min│
│  UGS路径: [___________________] [浏览...]                   │
│  填充间隔: 5.0 mm      |  Y偏移: 5.0 mm    |  Z深度: -2.0 mm │
│  [XZ轴交换 (X→Z, Z→X)]                                        │
├─────────────────────────────────────────────────────────────┤
│                    执行操作                                  │
│  切割刻字:  [提取描边] [刀路G代码] [开始切割]                │
│  UV填充:   [填充G代码] [开始填充]                            │
│  保存图案:  [保存SVG] [保存预览图]                           │
└─────────────────────────────────────────────────────────────┘
```

## 五、操作流程

### 步骤1：加载图片

- **方式一**：点击左侧「原图」区域，选择图片文件
- **方式二**：直接将图片拖拽到窗口中

支持格式：JPG、PNG、BMP、TIFF、WEBP

> 推荐使用**白底图片**，软件针对动漫人物轮廓提取进行了优化

### 步骤2：提取描边

1. 加载图片后，点击绿色按钮 **「提取描边」**
2. 等待处理完成，右侧会显示提取的轮廓预览

### 步骤3：调整参数

根据预览效果调整以下参数：

| 参数 | 范围 | 说明 |
|------|------|------|
| 白色阈值 | 150-255 | 背景白色的识别阈值，值越大越严格 |
| 偏移距离 | -20~20 | 正值向外扩展，负值向内收缩 |

调整偏移距离时，右侧预览会实时更新

### 步骤4A：切割操作（刻字）

1. 设置G代码参数：
   - **加工宽/高**: 目标切割尺寸（mm），系统会等比缩放
   - **进给速度**: 切割速度（mm/min）

2. 点击 **「刀路G代码」** 保存G代码文件

3. 点击 **「开始切割」** 自动启动UGS并加载文件

> 首次使用「开始切割」需先配置UGS路径

### 步骤4B：填充操作（UV雕刻）

1. 设置填充参数：
   - **填充间隔**: 扫描行间距（mm）
   - **Z深度**: 雕刻深度（负值，如 -2.0）

2. 点击 **「填充G代码」** 保存填充G代码

3. 点击 **「开始填充」** 启动UGS执行雕刻

### 步骤5：保存图案

- **「保存SVG」**: 导出可缩放的矢量图
- **「保存预览图」**: 保存当前预览的PNG图片

## 六、UGS路径配置

使用「开始切割」或「开始填充」功能前，需配置Universal Gcode Sender路径：

1. 点击「UGS路径」旁的 **「浏览...」** 按钮
2. 选择以下任一文件类型：
   - `.exe` 可执行文件
   - `.jar` Java程序文件
   - `.lnk` 快捷方式

### 下载UGS

如未安装UGS，可从以下地址下载：
- 官网：https://winder.github.io/ugs_website/

## 七、参数说明

### 基础参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 白色阈值 | 230 | 越大对白色要求越严格 |
| 偏移距离 | 0.0 | 刀具补偿，正值外扩 |

### G代码参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 加工宽 | 50 mm | UI基准宽度 |
| 加工高 | 76 mm | UI基准高度 |
| 进给 | 100 mm/min | 切割速度 |

### 填充参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 填充间隔 | 5.0 mm | 扫描线间距 |
| Z深度 | -2.0 mm | 雕刻深度 |

### 高级选项

- **XZ轴交换**: 勾选后交换X和Z轴映射（适配特殊机械结构）

## 八、坐标系统说明

软件使用专用切割机坐标系：

- **X轴**: 负值表示进纸，X=0为刀架最外侧
- **纸张中心**: X=-7, Y=4
- **加工区域**: X从-14到0（宽14mm），Y从0到8（高8mm）
- **自动旋转**: 系统会自动优化图形方向以适应加工区域

## 九、常见问题

**Q: 提取的轮廓不完整？**
A: 尝试降低「白色阈值」值（如从230降到200）

**Q: 切割尺寸不对？**
A: 检查「加工宽」和「加工高」参数，系统会等比缩放

**Q: UGS无法启动？**
A: 确保「UGS路径」已正确配置，且文件存在

**Q: 填充和切割对不上？**
A: 软件已自动确保两者使用相同坐标转换，请先生成刀路G代码再生成填充

## 十、文件说明

| 文件 | 功能 |
|------|------|
| `gui.py` | 主界面程序 |
| `outline_extractor.py` | 轮廓提取核心 |
| `gcode_generator.py` | 切割路径生成 |
| `fill_generator.py` | UV扫描填充生成 |
| `svg_generator.py` | SVG矢量图输出 |

---

如有其他问题，请检查软件状态栏的提示信息。
